[package]
name = "carmine_protocol"
version = "0.1.0"
description = "Code for Carmine Option AMM"
# cairo-version = "2.0.0-rc"
# homepage = "https://github.com/auditless/cairo-template"

# [scripts]
# test = "cairo-test ."
# test = "protostar test"
# Add your own custom commands and run them with `scarb run <command>`

[dependencies]
cubit = { path = "../cubit" }
